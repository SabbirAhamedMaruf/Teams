import { IconButton } from "@chakra-ui/react";
import { LuUserRound } from "react-icons/lu";
const User = () => {
  return (
    <IconButton>
      <LuUserRound />
    </IconButton>
  );
};
export default User;
